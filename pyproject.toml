[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "agri-rag-agent"
version = "0.3.0"
description = "荟诊 · 芦荟病虫害智能问答与诊断 Agent（RAG + Function Calling）"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "numpy>=1.22",
    "fastapi>=0.100",
    "uvicorn>=0.23",
    "httpx>=0.24",
    "jieba>=0.42",
]

[project.optional-dependencies]
dev = ["pytest>=7.4"]

[project.scripts]
agri-agent = "agri_agent.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
addopts = "-q"
